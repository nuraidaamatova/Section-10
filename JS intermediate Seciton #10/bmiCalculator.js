// function bmiCalculator(weight, height) {
//     let bmi = weight / (height * height);
//     if (bmi <= 18.5) {
//         document.write(`Your Bmi is ${Math.round(bmi)}, so you are underweight.`);
//     } else if (bmi > 18.5 && bmi <= 24.9) {
//         document.write(`Your Bmi is ${Math.round(bmi)}, so you have normal weight.`);
//     } else if (bmi > 24.9) {
//         document.write(`Your Bmi is ${Math.round(bmi)}, so you are overweight.`);
//     }
//     // return Math.round(bmi);


// }
// let bmi = bmiCalculator(65, 1.8);
// bmiCalculator();